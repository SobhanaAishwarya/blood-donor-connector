Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==6.0.5
PyJWT==2.13.0
python-dotenv==1.2.3

# Production WSGI server (used by Procfile / render.yaml). Not used by
# `python run.py` in local dev, and not runnable on Windows - that's fine,
# it's only invoked on the (Linux) host.
gunicorn==23.0.0
